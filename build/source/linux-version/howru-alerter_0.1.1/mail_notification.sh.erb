#!/bin/bash
SUBJECT="{subject}"  
RECEIVER="{receiver}"
TEXT="{text}"

SERVER_NAME=$HOSTNAME  
SENDER=$(whoami)  
USER="noreply"

MAIL_TXT="Subject: $SUBJECT\nFrom: $SENDER\nTo: $RECEIVER\n\n$TEXT"  
echo -e $MAIL_TXT | sendmail -t  
exit $?  
